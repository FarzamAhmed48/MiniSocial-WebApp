<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <a href="/logout" class="bg-red-500 px-3 py-2 hover:underline rounded-sm inline-block mb-3">Logout</a>
        <div class="flex gap-5 items-center">
            <div class="h-20 w-20  rounded-full overflow-hidden">
                <img class="w-full h-full object-cover " src="/images/uploads/<%=user.profilepic%>" alt="">
            </div>
            <h3 class="text-3xl mb-3">Hi, <%=user.name%></h3>
        </div>
        <h4 class="mb-5">You can create a new post</h4>
        <form action="/post" method="post">
            <textarea placeholder="What's on your mind?" class=" px-5 py-2 w-1/3 resize-none bg-transparent border-2 border-zinc-800 block " name="content" id=""></textarea>
            <input type="submit" value="Create Post" class="px-5 py-2 bg-blue-500 block mt-3">
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5 mb-2">

                <%user.posts.reverse().forEach(function(item){%>
                    <div class="w-1/3 post p-4 mb-4 border-[1px] bg-zinc-800 border-zinc-700">
                        <h4 class="text-blue-500 mb-3"><%= user.username %></h4>
                        <p class="text-sm tracking-tight"><%=item.content%></p>
                        <small class="inline-block mb-1"><%=item.likes.length%> likes</small>
                        <div class="flex mt-5 btns gap-4">
                            <a href="/like/<%=item._id%>" class="text-blue-500"><%=item.likes.indexOf(user._id) === -1 ? "Like": "Unlike"%></a>
                            <a href="/edit/<%=item._id%>" class="text-zinc-600">Edit</a>
                        </div>
                    </div>
                    <%})%>


                
            </div>
        </div>
    </div>
</body>
</html>